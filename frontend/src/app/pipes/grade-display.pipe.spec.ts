import { GradeDisplayPipe } from './grade-display.pipe';

describe('GradeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new GradeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
